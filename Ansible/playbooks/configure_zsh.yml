- name: Install and cofigure zsh
  hosts: "rpi_build"
  become: yes
  tasks:
    - name: Install ZSH
      become: yes
      apt:
        name: zsh
        state: latest

    # This bit doesn't work!! Need to fix
    - name: Install oh-my-zsh
      shell:
        cmd: $(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)

    # Change shell to zsh and configure
    - name: change user shell to zsh
      user:
        name: pi
        shell: /usr/bin/zsh

    - name: copy zsh config
      copy:
        src: "{{ item }}"
        dest: /home/pi
        owner: pi
        group: pi
        mode: '644'
      with_fileglob: "{{ scripts }}/config/.z*" # Path need updating
      